# Swagger proof
Run `uvicorn app.main:app --reload` and open `http://127.0.0.1:8000/docs`.
Capture: authentication routes, protected Notes routes, login token response, and authenticated CRUD response.
